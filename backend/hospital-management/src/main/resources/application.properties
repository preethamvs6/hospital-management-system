# ============================================
# Hospital Management System — Application Properties
# ============================================

# Active Profile (switch to 'prod' for production)
spring.profiles.active=dev

# Application Name
spring.application.name=hospital-management

# Server Port
server.port=8080
